server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/audiomind
    username: postgres
    password: 12345

  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 200MB

  jpa:
    hibernate:
      ddl-auto: update